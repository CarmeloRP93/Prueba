{{ form_start(form, {'attr': {'id': id|default('form')}} ) }}


    {{ form_widget(form) }}
    <input type="button" value="{{ 'Eliminar' }}" class="btn btn-danger " style="height: 30px; width: 250px;" 
           onclick="return confirmarEliminar()" >

{{ form_end(form) }}

<script>
    function confirmarEliminar() {
        bootbox.confirm ('¿Estas seguro?', function(result) {
           if (result === true) {
               document.getElementById("form").submit();
           } 
        });
    }
</script>
