{% extends 'modulomonitoresmonitoresBundle::main.html.twig' %}

{% block title %} {{monitor.nombre}} {% endblock %}
{% block contenido %}
    <div class="usuardesc">
        <div class="accionesExclus">
            <a href="{{path('modulomonitores_monitores_editarmiperfilm')}}" class="btn btn-primary">Editar perfil</a> 
        </div>
        <h1>Monitor: {{monitor.nombre}}</h1>
        <div class="usuarfot">
            <img src="{{ asset('images/no-image-found.png') }}" style="float: left; width: 100%;" />        
        </div>
        <div class="usuardat">
            <h4>F.Nacimiento: </h4> <p>{{monitor.fNacimiento}}</p>
            <h4>Correo electrónico: </h4> <p>{{monitor.email}}</p>
            <h4>Registro: </h4> <p>{{monitor.registro|date('d/m/Y H:i')}}</p>
            <h4>Telefono: </h4> <p>{{monitor.telefono}}</p>
        </div>
    </div>
{% endblock %}