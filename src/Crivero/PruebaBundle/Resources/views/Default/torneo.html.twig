{% extends 'CriveroPruebaBundle::main.html.twig' %}

{% block title %}Vista de {{torneo.nombre}} {% endblock %}
{% block contenido %}
    <div class="container text-center">
        <h2>Detalles del Torneo</h2>
        {% if torneo.estadoTorneo == "Solicitud Presentada" %}
            <button class="btn btn-default">Aceptar Solicitud</button>
            <button class="btn btn-danger">Rechazar Solicitud </button>
        {% endif %}
        {#{% block detalles %}
            {% if torneo.estadoTorneo == "Validado" %}
                <h1>Equipos</h1>
                    {% for equipo in equipos %}
                        {%if equipo.idTorneo ==torneo.id%}
                            <a href="{{url('crivero_prueba_equipo', {'id': equipo.id})}}" class="fila {{cycle(["par","impar"], loop.index)}}">Clasificación: {{equipo.clasificacion}}º-Nombre del equipo:{{ equipo.nombre }}-Puntos:{{equipo.puntuacion}}</a>
                        {%endif%}
                    {% endfor %}
            {% endif %} 
        {% endblock %}#}
        
        {% if torneo.estadoTorneo == "Validado"%}
            <h3>Insertar tabla de liga del deporte en cuestión (fútbol,baloncesto, tenis)</h3>
            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th> Posición </th>
                            <th> Equipo  </th>
                            <th> Puntos   </th>
                            <th> Victorias </th>
                            <th> Derrotas </th>
                            <th> Empates  </th>
                        </tr>
                    </thead>
                    {% for equipo in equipos %}
                        {%if equipo.idTorneo ==torneo.id%}
                        <tr>
                            <td>  {{equipo.clasificacion}}º  </td>
                            <td>  <a href="{{url('crivero_prueba_equipo', {'id': equipo.id})}}">{{equipo.nombre}}</a> </td>
                            <td>   {{equipo.puntuacion}}   </td>
                            <td>   {{equipo.victorias}}   </td>
                            <td>   {{equipo.derrotas}}   </td>
                            <td>   {{equipo.empates}}   </td>
                        </tr>
                        {%endif%}
                    {%endfor%}
                </table>
            </div>
        {%endif%}
        
    </div>
{% endblock %}